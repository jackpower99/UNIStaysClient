import React from 'react'

export default function propertyDetails(props) {

    const { landlord_id, address, colleges, size_sq_meters, description, bedroom_count, bathroom_count, property_type, available_start, available_end, price, property_images, amenities, ...rest} = props

    return (
    <div>index</div>
  )
}
